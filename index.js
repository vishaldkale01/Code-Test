const _ = require('lodash');
let model = {
  xyz: 'vishal',
  c: 'c',
  new: '',
};
let arr = ['vishal', 'kale', 'dfdfsf'];
let res = (model.nee = _.each(arr, (val) => {
  return model.val;
}));
console.log(model);
console.log('rs,', res);
